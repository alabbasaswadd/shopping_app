import 'package:flutter/material.dart';

final lightTheme = ThemeData.light().copyWith(cardColor: Colors.amber);

final darkTheme =
    ThemeData.dark().copyWith(iconTheme: IconThemeData(color: Colors.amber));
